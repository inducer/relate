extends: default

# This isn't the goal state, but there's an unmanageable amount of warnings
# without some compromises right now.

rules:
  line-length:
    max: 200
    level: warning
  document-start: disable
  indentation: disable
  empty-lines: disable
  commas: disable
  hyphens: disable
  comments: disable
